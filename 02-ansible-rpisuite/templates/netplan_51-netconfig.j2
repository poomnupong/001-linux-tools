network:
  version: 2
  ethernets:
    eth0:
      dhcp4: false
      addresses: [{{ NODE_IP_ADDR }}]
      gateway4: {{ NODE_DEFAULTGW }}
      nameservers:
        addresses: [{{ NODE_NAMESERVERS }}]
